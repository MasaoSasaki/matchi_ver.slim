<h1>店舗一覧</h1>
<table>
  <thead>
    <tr>
      <td>店舗名</td>
      <td>メニュー数</td>
    </tr>
  </thead>
  <tbody>
    <%= @restaurants.each do |restaurant| %>
      <td><%= retaurant.name %></td>
      <td><%= @menus.where(restaurant_id: [retaurant.id]).count %></td>
    <% end %>
  </tbody>
</table>