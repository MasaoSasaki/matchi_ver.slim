<div class="devise-form">
  <h2>店舗ログイン</h2>
  <small>※PCからの利用を推奨します。スマートフォンの場合、表示が崩れる可能性があります。</small>
  <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
    <div class="field">
      <%= f.label :email, value: 'メールアドレス' %><br />
      <%= f.email_field :email, autofocus: true, autocomplete: "email", value: 'japanese@jp' %>
    </div>

    <div class="field">
      <%= f.label :password, value: 'パスワード' %><br />
      <%= f.password_field :password, autocomplete: "current-password", value: '000000' %>
    </div>

    <% if devise_mapping.rememberable? %>
      <div class="field">
        <%= f.check_box :remember_me %>
        <%= f.label :remember_me, value: 'パスワードを保持する。' %>
      </div>
    <% end %>

    <div class="actions">
      <%= f.submit "ログイン" %>
    </div>
  <% end %>

  <%= render "owner/restaurants/shared/links" %>

</div>