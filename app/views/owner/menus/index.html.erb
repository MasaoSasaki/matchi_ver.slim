<div class="menus-index">
  <h1>Menus</h1>
  <table>
    <thead>
      <tr>
        <th>メニューID</th>
        <th>イメージ画像</th>
        <th>メニュー名</th>
        <th>予約状況</th>
        <th>販売状況</th>
      </tr>
    </thead>
    <% @menus.each do |menu| %>
      <tbody>
        <tr>
          <td><%= menu.id %></td>
          <td><%#= attachment_image_tag :menu_image %></td>
          <td><%= link_to "#{menu.title}", owner_restaurant_menu_path(current_owner_restaurant, menu) %></td>
          <td><%= menu.reservation_method %></td>
          <td>
            <% if menu.is_sale_frag == true %>
              <%= '販売中' %>
            <% else %>
              <%= '停止中' %>
            <% end %>
          </td>
        </tr>
      </tbody>
    <% end %>
  </table>
</div>
